// Top-level build file. Repositories are configured in settings.gradle.kts
// and project-level build files must not add repositories when
// dependencyResolutionManagement.repositoriesMode = RepositoriesMode.FAIL_ON_PROJECT_REPOS

// Keep this file intentionally minimal.
